class Parser {
    constructor (tokens) {
        this.tokens = tokens;
    }

    parse() {
        let tree = {};

        return tree;
    }

    static printSyntaxTree(tree) {
        return '';
    }
}